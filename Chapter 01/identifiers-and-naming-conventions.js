// Valid identifiers
let myVariable = 10;
let _privateVariable = "secret";
let $element = document.getElementById("myElement");

// Invalid identifiers
let 1stVariable = 10; // Invalid: starts with a digit
let my-var = 20; // Invalid: contains a hyphen (-)
let if = "condition"; // Invalid: 'if' is a reserved keyword
